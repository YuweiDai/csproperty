export class OverViewModel {
        totalCount:number;

        constructCount:number;

        landCount:number;

        totalPrice:number;

        constructPrice:number;

        landPrice:number;

        constructArea:number;

        landArea:number;
        totalArea:number;
        
}

